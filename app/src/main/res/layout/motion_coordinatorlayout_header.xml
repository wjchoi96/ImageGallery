<?xml version="1.0" encoding="utf-8"?>
<com.gallery.kakaogallery.CollapsibleToolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/motionLayout"
    android:minHeight="50dp"
    android:fitsSystemWindows="false"
    app:layout_scrollFlags="scroll|enterAlways|snap|exitUntilCollapsed"
    app:layoutDescription="@xml/scene_09"
    ><!--app:layout_behavior="@string/appbar_scrolling_view_behavior" 얘는 안해주고 scroll content 쪽에서만 해주면 된다 -->

    <ImageView
        android:id="@+id/background"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@color/mainColor"
        android:scaleType="centerCrop"
        android:src="@drawable/background_motion_header" />

    <TextView
        android:id="@+id/label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Monterey"
        android:textColor="#000000"
        android:textSize="32sp"
        android:transformPivotX="0dp"
        android:transformPivotY="0dp" />
</com.gallery.kakaogallery.CollapsibleToolbar>